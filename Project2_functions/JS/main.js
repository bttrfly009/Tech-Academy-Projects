function myFunction() {
    var sentence = "Roses are red,"; //Sets first variable
    sentence += " and violets are blue."; //Adds to it
    document.getElementById("Concatenate").innerHTML = sentence;
}